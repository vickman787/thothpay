{
  "name": "citeflow-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing CiteFlow AI's x402-payable research endpoint as an agent tool. Self-contained — copy this folder anywhere, run `npm install`, and point your MCP client at it.",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@circle-fin/x402-batching": "^3.2.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  }
}
